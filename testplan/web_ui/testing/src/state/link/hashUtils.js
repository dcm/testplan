import { createHashHistory } from 'history';
import { createMemoryHistory } from 'history';
import { createBrowserHistory } from 'history';
import { parsePath } from 'history';
import { createLocation } from 'history';
import { createPath } from 'history';
import { onLocationChanged } from 'connected-react-router/esm/actions';

const hHist = createHashHistory();
const mHist = createMemoryHistory();
const bHist = createBrowserHistory();

// export const pushHash

export const hashRouterActions = {

};
